.canvas-wrap { position: absolute; inset: 0; border-radius: 32rpx; overflow: hidden; opacity: 0; pointer-events: none; transition: opacity .2s ease; }
.canvas-wrap.show { opacity: 1; }
.living-canvas { width: 100%; height: 100%; }
