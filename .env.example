# ===== AI句灵 服务端配置（复制为 .env 使用，所有项都有安全默认值，可以不配直接跑） =====

# 监听端口
PORT=3000

# SQLite 数据库文件位置（默认 var/jvling.sqlite；生产可迁移 PostgreSQL，见 docs/DATABASE.md）
# DB_PATH=var/jvling.sqlite

# 会话签名密钥（不配置时首次启动自动生成并持久化到数据库 settings 表）
# APP_SECRET=please-change-me

# ===== 大模型配置（两种经营模式，按需选其一） =====
# 模型解析顺序：用户自带 Key → 平台公共 Key（下方）→ 本地规则引擎。
#
# 【省心模式 · 面向不想折腾的用户，推荐】给平台配一个公共 Key：所有订阅用户【无需任何配置】
#   即可用大模型、开箱即用（每会员每天 80 次，成本由你承担、按上限封顶）；重度用户仍可自带 Key 不限量。
# 【纯 BYOK 模式】下面全部留空：平台不出模型成本，用户必须在网页【模型设置】自带 Key 才用大模型。
#
# 任意 OpenAI 兼容接口均可（火山方舟/DeepSeek/通义/OpenAI…）。Key 只放服务端、永不下发前端。
# 配置后运行 node scripts/llm-test.mjs 一键自检。
# LLM_PROVIDER=none           # none=纯BYOK；填 ark|deepseek|openai… 即开启省心模式
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# LLM_API_KEY=
# LLM_MODEL_DEFAULT=doubao-seed-1-6-flash-250615
# LLM_MODEL_PREMIUM=doubao-seed-1-6-250615

# 模型单价（元/千 token，用于成本统计；按你实际开通的价格改）
# LLM_PRICE_DEFAULT_IN=0.00015
# LLM_PRICE_DEFAULT_OUT=0.0015
# LLM_PRICE_PREMIUM_IN=0.0008
# LLM_PRICE_PREMIUM_OUT=0.008

# AI 暖场每日成本上限（元）。超过后当天暖场自动只用本地模板，不再调用大模型
# WARMUP_DAILY_BUDGET=2.0

# 管理员初始账号（首次启动种子化；务必上线前修改）
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=jvling-admin-2026
