# 极速去水印 · 微信小程序

粘贴抖音 / 快手 / 小红书 / 微博 / B站 / 皮皮虾 的分享链接，一键提取**无水印**视频或图片并保存到相册。**靠广告变现**：保存前看一条激励视频广告，配合 Banner / 插屏广告，对接微信「流量主」即可产生收入。内置**新人免广告 + 分享得次数**的增长玩法、解析记录云端同步、以及按 openid 鉴权的**数据看板**。

> 这是一个独立项目，位于本仓库的 `dewatermark/` 目录，与仓库里的「AI句灵」社交项目互不相干。

## 为什么是「云开发」架构

去水印小程序最大的两个上线障碍是 **request 合法域名要 ICP 备案** 和 **下载域名白名单**。本项目用**微信云开发**：

- 解析逻辑放在**云函数**里跑，客户端用 `wx.cloud.callFunction` 调用，**不走 request 域名校验**，省掉给抖音等一堆域名做备案的麻烦；
- 视频可选**转存云存储**（`*.tcb.qcloud.la` 域名自动在 `downloadFile` 白名单内），解决直链下载被拦的问题；
- 无需自建服务器，按量计费，个人开发者也能起步。

## 目录结构

```
dewatermark/
├── miniprogram/                小程序前端
│   ├── app.js / app.json / config.js   配置入口（云环境 ID、广告位 ID 在 config.js）
│   ├── pages/
│   │   ├── index/    首页：粘贴链接 → 识别平台 → 解析
│   │   ├── result/   结果页：预览 +「看广告解锁下载」(变现核心)
│   │   ├── batch/    批量解析：多链接并发解析，看一次广告全部保存
│   │   ├── history/  历史记录（本地缓存 + 云端同步）
│   │   ├── about/    我的：协议 / 隐私 / 版权 / 反馈
│   │   ├── doc/      协议文档承载页
│   │   └── admin/    数据看板（连点版本号进入，按 openid 鉴权）
│   └── utils/        cloud / ad / link / save / store / platform / gate(额度闸门) / quota / share
├── cloudfunctions/
│   ├── parse/        核心解析云函数
│   │   ├── parsers/  douyin / kuaishou / xiaohongshu / weibo / bilibili / pipixia + 调度
│   │   ├── lib/      http(零依赖) / result(结构) / thirdparty(第三方兜底) / extract(直链抽取)
│   │   └── index.js  入口：内置解析 → 第三方兜底 →(可选)转存云存储 → 统计
│   ├── login/        静默登录 + 用户统计
│   ├── stats/        数据看板聚合（按 ADMIN_OPENIDS 鉴权）
│   ├── history/      解析记录云端同步（add/list/clear，按 openid）
│   └── quota/        免广告额度 + 分享奖励 + 邀请归因（get/spend/reward/bind，按 openid）
├── scripts/test.js   离线逻辑自测（不联网）
└── docs/             DEPLOY 部署 / REVIEW 上架避坑 / MONETIZE 变现配置
```

## 快速开始

1. **导入项目**：微信开发者工具 → 导入 → 选择 `dewatermark/` 目录，填入你的小程序 AppID。
2. **开通云开发**：工具顶部「云开发」→ 开通 → 新建环境，复制**环境 ID**。
3. **填配置**：把环境 ID 和广告位 ID 填进 `miniprogram/config.js`。
4. **部署云函数**：右键 `cloudfunctions/parse`、`login`、`stats`、`history`、`quota` → 上传并部署（云端安装依赖）。
5. **建数据库集合**：云开发控制台新建集合 `users`、`parse_logs`、`histories`、`credits`（权限选「仅创建者可读写」即可）。
6. 真机预览，粘贴一条抖音分享链接测试。

详细步骤见 **[docs/DEPLOY.md](docs/DEPLOY.md)**。

## 本地自测

解析的纯逻辑（链接识别、抖音 `_ROUTER_DATA` 提取、meta 解析、结果规范化）都有离线单测，不需要联网：

```bash
cd dewatermark && npm test
```

## 变现（靠广告收入）

- **激励视频**：`result` 页点「保存」前播放，看完才解锁下载——这是主要收入来源。
- **Banner**：首页 / 结果页底部横幅。
- **插屏**：保存成功后偶发弹出（可在 `config.js` 留空关闭）。
- **新人免广告 + 分享得次数 + 邀请裂变**：`quota` 云函数按 openid 发放免广告额度，扣减优先级「每日免费 → 免广告额度 → 看广告」；分享链接带 `?ref=openid`，新人进入登录后归因，**邀请人与新人各得奖励**（一次性绑定 / 仅新账号可归因 / 每日封顶防刷）。调参与取舍见 [docs/MONETIZE.md](docs/MONETIZE.md)。

开通「流量主」需要小程序**累计独立访客 ≥ 1000**且类目合规。广告位创建、收益预估、策略调优见 **[docs/MONETIZE.md](docs/MONETIZE.md)**。

## 数据看板

在「我的」页**连点版本号 5 次**进入数据看板，看：累计/今日/近 7 日解析量、各平台占比、解析来源（内置 vs 第三方）、总用户/新增/活跃/复访率。

权限由 `stats` 云函数按 **openid 白名单**校验：在 `stats` 云函数环境变量 `ADMIN_OPENIDS`（逗号分隔）里填入管理员 openid 即可。未配置时，看板会显示你当前的 openid 方便复制填入。

## 批量解析与云端同步

- **批量解析**（首页「批量解析一次搞定」入口）：一次粘贴多条链接（≤ 20），并发解析（并发 2，避免压垮平台），逐条显示状态；「全部保存」看**一次**激励视频即可解锁本批全部下载。
- **云端同步**：解析记录除本地缓存外，会通过 `history` 云函数按 openid 存到 `histories` 集合；历史页打开时拉取云端并与本地**合并去重**，换设备 / 重装也不丢。清空会同时清云端。

## ⚠️ 上架风险必读

去水印类目在微信审核里**有版权敏感性**，纯"搬运他人作品"定位容易被拒。本项目已内置版权/免责声明、把产品定位为"**自有素材提取助手**"。如何选类目、写审核备注、应对常见拒绝理由，**务必先读 [docs/REVIEW.md](docs/REVIEW.md)**。

## 合规与边界

- 解析依赖各平台公开页面结构，平台升级后可能失效，需持续维护；生产建议配置第三方聚合接口兜底（见 DEPLOY）。
- 协议文本为通用模板，正式上线前请交由法务复核。
- 本工具不存储、不分发任何作品，仅提供技术便利；请引导用户仅处理自己有权使用的内容。
