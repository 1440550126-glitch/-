# 猫约时光 · 上线前待办清单（后端 / 资质 / 合规）

当前为**前端原型**：数据存本地缓存、TA 的状态/支付/实名均为模拟。**已移除全部陌生人社交（聊天大厅/匹配/陌生人私聊），现为纯情侣 app；后端方案改为云开发。** 下面是从原型到正式上线要补齐的事项，按优先级排列。

## 一、后端与账号（云开发 · 已接入双向同步）
- [x] 云开发初始化已加入 `app.js`（填 `CLOUD_ENV` 后生效）。
- [x] 情侣关系 + 共享数据**双向同步**：云函数 `coupleData`(bind/pull/sync/unbind) + `store.js` 同步层。邀请码绑定、离线优先、删除墓碑、余额按流水重算。详见 `docs/CLOUD-SYNC.md`。
- [x] 已同步（视角无关）：愿望 / 纪念日 / 回忆 / 猫 / 小金库 / 菜单 / 在一起日期。
- [x] 心情 / 每日一问 / 纸条：**按作者(openid)拆分后已同步**（存绝对作者、读时翻译成 self/partner，不会身份对调）。
- [ ] 昵称 / 头像：各自的称呼偏好，保持本地不同步（如需统一可后续加）。
- [ ] 部署：开通云开发 → 建集合 `couples` → 部署 `coupleData`、`secCheck`（见 `docs/CLOUD-SYNC.md`）。
- [ ] 解绑会员校验：上线后在云函数侧再校验一遍（前端不可信）。

## 二、实时同步与推送（"陪伴/状态"的核心）
- [x] 双方共享数据 + 心情/纸条/每日一问互通（云开发）。
- [x] **准实时**：前台 `startRealtime`——优先云数据库 `watch()`，无权限自动回退 10s 轮询 `ping`（默认部署即"约 10s 内可见"）。
- [x] **TA 的电量/在线/位置**：双方 `reportPresence` 真实上报（位置默认不共享，可手动开启）；**戳一戳**经同步通道实时送达并弹提示。详见 `docs/CLOUD-SYNC.md`。
- [ ] 想要"即时"推送：给 `couples` 配安全规则 `{"read":"doc.members.includes(auth.openid)"}` 启用 watch。
- [ ] **订阅消息**：App 关闭时的关心提醒/戳一戳/纪念日推送——需在后台申请模板并引导授权。

## 三、地理位置（地图功能）
- [ ] `wx.getLocation` / `wx.onLocationChange`：在小程序后台「开发管理 → 接口设置」申请**地理位置接口**权限（需说明用途，个人主体有使用范围限制）。
- [ ] `app.json` 已声明 `requiredPrivateInfos: ["getLocation"]`，需配套**隐私保护指引**（用户首次使用弹授权）。
- [ ] 位置共享要可关闭、可模糊（只显示城市/距离），尊重隐私。

## 四、支付（会员）
- [x] **微信支付**已接入：云函数 `pay`（`cloud.cloudPay.unifiedOrder` + 回调发放）+ `utils/pay.js` + 会员页接线；未配置商户时自动降级"模拟开通"。详见 `docs/PAYMENT.md`。
- [ ] 上线前：开通商户号 → 云开发后台绑定 → 填 `pay/index.js` 的 `PAY_CONFIG` → `utils/pay.js` 的 `PAY_ENABLED=true` → 建 `users` 集合并部署 `pay`。
- [ ] 退款/对账/自动续费按需补充；会员态以服务端 `users` 为准（客户端只是缓存）。

## 五、实名认证
- [ ] 对接权威核验（运营商三要素 / 公安/人脸核身等持牌渠道），**不要自己存明文证件号**。
- [ ] 敏感信息加密存储、最小化收集、单独授权与告知。

## 六、内容安全
- [x] 文本内容安全已实现：云函数 `cloudfunctions/secCheck`（`msgSecCheck`）+ 封装 `utils/seccheck.js`，已接入纸条 / 愿望 / 昵称。
- [ ] 部署生效：开通云开发 → 部署 `secCheck` → 申请内容安全接口（见 `UPLOAD-GUIDE` 三、四）。
- [ ] 回忆图片如需校验：用 `secCheck` 的 `imgSecCheck` 分支（已留接口）。

## 七、隐私与法律合规
- [x] 《用户协议》《隐私政策》《社区规范》《未成年人保护》——已写好并接入小程序（**我的 → 设置**，页面 `pages/legal/legal`），文本另存 `docs/legal/`。主体名称已填 **郑州市郑东新区星阙云网络工作室**，对外邮箱 `zhishiliu057@gmail.com`。
- [ ] 小程序后台「用户隐私保护指引」逐项声明（位置、相册、设备信息等）——照 `docs/PRIVACY-GUIDE.md` 对照表填。
- [x] 数据删除 / 撤回授权：已有"清空所有数据"（本地）；云端上线后补"账号注销 / 数据导出"。
- [x] **未成年人保护**：已在协议与专页声明，未成年人须监护人同意陪同使用。
- ➕ 上架步骤手册见 `docs/UPLOAD-GUIDE.md`（按企业主体整理）。

## 八、类目资质
- [x] ~~「婚恋交友」类目资质~~ —— **已移除陌生人社交，不再需要**；选普通类目（工具 / 社交-记录）即可。
- [ ] 含地图、支付，仍有各自的接口/类目要求。
- [ ] 大陆上线需 ICP 备案。

## 九、虚拟道具 / 小金库合规
- [ ] 当前「小金库」是**记账本**（用户手动记数字，无真实资金流），合规风险低。
- [ ] 若将来改为**真实充值/提现**：涉及支付牌照、反洗钱、防赌博等金融监管，务必法务评估，**不要**做成可提现的虚拟币。
- [ ] 金币掉落/开库金光等仅作视觉表现，避免诱导付费、避免抽奖/赌博观感。

## 十、资源与体验
- [ ] 替换合成占位音效（`assets/audio/*.mp3`）为成品音效；保留文件名即可（单个资源须 <200K）。
- [ ] 地图头像标记可做成"用户头像图"动态生成（目前是通用粉/紫水滴 pin）。
- [ ] App 图标、分享图、引导页、骨架屏、空状态插画。
- [ ] 分包加载（页面已不少）、错误上报、埋点统计。

## 十一、测试与上架
- [ ] 多机型真机测试（定位、电量、音频、动画性能）。
- [ ] 体验版 → 隐私检测 → 提交审核（备齐类目资质与协议）。
- [ ] 灰度发布与监控。

---

> 一句话：原型已经把**交互和体验**跑通了；上线的工作量主要在**后端实时同步 + 各类资质/合规**。建议先确定主体与「婚恋交友」类目资质，再排后端与支付。
