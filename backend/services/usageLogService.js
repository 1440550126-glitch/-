const {run,now}=require('../db'); const {id}=require('../utils/ids'); const profitService=require('./profitService');
function recordUsage({userId,projectId=null,shotId=null,jobId=null,provider,model,taskType,requestId='',providerTaskId='',usage={},estimatedCost=0,actualCost=0,userCharge=0,status='succeeded',errorCode='',errorMessage=''}){ const profit=userCharge-actualCost; const margin=userCharge?profit/userCharge:0; run('INSERT INTO api_usage_logs (id,user_id,project_id,shot_id,job_id,provider,model,task_type,request_id,provider_task_id,input_tokens,output_tokens,estimated_cost,actual_cost,user_charge,profit,profit_margin,currency,status,error_code,error_message,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)',[id('api'),userId,projectId,shotId,jobId,provider,model,taskType,requestId,providerTaskId,usage.input_tokens||0,usage.output_tokens||0,estimatedCost,actualCost,userCharge,profit,margin,'USD',status,errorCode,errorMessage,now()]); run('INSERT INTO provider_cost_logs (id,provider,model,task_type,actual_cost,currency,raw_usage_json,created_at) VALUES (?,?,?,?,?,?,?,?)',[id('cost'),provider,model,taskType,actualCost,'USD',JSON.stringify(usage),now()]); profitService.record({userId,projectId,jobId,actualCost,userCharge}); }
module.exports={recordUsage};
