const fs=require('fs'); const path=require('path'); const {run,now}=require('../db'); const {id}=require('../utils/ids');
async function extractFrames({projectId,shotId,videoUrl,duration=5,level='advanced'}){ const frames=[]; const times=level==='basic'?[0,duration]:Array.from({length:Math.max(2,Math.ceil(duration)+1)},(_,i)=>Math.min(i,duration)); for(const t of times){ const type=t===0?'first_frame':t===duration?'last_frame':'keyframe'; const url=`/storage/frames/${projectId}_${shotId}_${type}_${t}.svg`; const full=path.join(process.cwd(),url); fs.writeFileSync(full,`<svg xmlns="http://www.w3.org/2000/svg" width="1280" height="720"><rect width="100%" height="100%" fill="#08070b"/><text x="80" y="120" fill="#d7b46a" font-size="42">LingMirror ${type} ${t}s</text><text x="80" y="190" fill="#fff" font-size="24">${projectId} / ${shotId}</text></svg>`); run('INSERT INTO memory_frames (id,project_id,shot_id,frame_type,frame_time_sec,frame_url,analysis_json,created_at) VALUES (?,?,?,?,?,?,?,?)',[id('frame'),projectId,shotId,type,t,url,'{}',now()]); frames.push({type,time:t,url}); } return frames; }
module.exports={extractFrames};
