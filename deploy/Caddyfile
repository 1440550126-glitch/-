# 把 your-domain.com 换成你的域名。Caddy 会自动签发并续期 HTTPS。
# 安装：https://caddyserver.com/docs/install  → 放到 /etc/caddy/Caddyfile → systemctl reload caddy
your-domain.com {
	encode zstd gzip
	reverse_proxy localhost:3000 {
		# 作战室是 SSE 实时直播：flush_interval -1 关闭响应缓冲，保证逐字推送
		flush_interval -1
	}
}
