# 追加到你现有 Caddyfile（不要改其它站点）。Caddy 会自动为该域名签发/续期 HTTPS。
# 重载：caddy reload --config /etc/caddy/Caddyfile   （或 systemctl reload caddy）
agent.lingmirror.com.cn {
	encode zstd gzip
	reverse_proxy 127.0.0.1:3003 {
		# 作战室是 SSE 实时直播：flush_interval -1 关闭响应缓冲，保证逐字推送
		flush_interval -1
	}
}
