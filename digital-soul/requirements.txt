# ---------- 核心（必需）----------
PyYAML>=6.0

# ---------- 本地大模型（推荐）----------
# 不需要 Python 包，安装 Ollama 即可：https://ollama.com
# 然后拉一个 16G 内存能跑的 4-bit 量化模型，例如：
#     ollama pull qwen2.5:7b-instruct
# 没装 Ollama 也能跑，框架会自动进入降级模式。

# ---------- 可选：更聪明的记忆检索（语义向量）----------
# 不装则自动使用内置词法检索（对中文也有效）。
# sentence-transformers>=2.2.0

# ---------- 可选：图片 / 视频认人（人脸识别）----------
# face_recognition>=1.3.0
# opencv-python>=4.8.0      # 仅视频认人需要

# ---------- 可选：PDF 文档摄取 ----------
# pypdf>=4.0.0
