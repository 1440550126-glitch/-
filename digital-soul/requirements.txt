# ---------- 核心（必需）----------
PyYAML>=6.0

# ---------- 桌面界面 ----------
# tkinter 是 Python 自带的；Linux 若缺失：sudo apt install python3-tk

# ---------- 本地大模型（推荐）----------
# 不需要 Python 包，安装 Ollama 即可：https://ollama.com
# 然后拉一个 16G 内存能跑的 4-bit 量化模型，例如：
#     ollama pull qwen2.5:7b-instruct
# 没装 Ollama 也能跑，框架会自动进入降级模式。

# ---------- 可选：更聪明的记忆检索（语义向量）----------
# 不装则自动使用内置词法检索（对中文也有效）。
# sentence-transformers>=2.2.0

# ---------- 可选：图片 / 视频认人（人脸识别，二选一）----------
# face_recognition>=1.3.0        # 精度高，依赖 dlib（编译较重）
# opencv-contrib-python>=4.8.0   # 树莓派友好，免 dlib（LBPH）；设 DSOUL_FACE_BACKEND=opencv
# （opencv-contrib-python 已含摄像头/视频读取；只用 dlib 版时再单装 opencv-python）

# ---------- 可选：语音输入输出 ----------
# faster-whisper>=1.0.0    # 语音转文字（本地，int8 量化模型很小）
# pyttsx3>=2.90            # 文字转语音（离线）
# sounddevice>=0.4.6       # 麦克风录音
# numpy>=1.24

# ---------- 可选：接入 ROS2 机器人 ----------
# 需要系统级 ROS2 (rclpy)，见 https://docs.ros.org（不是 pip 包）

# ---------- 可选：LoRA 微调（逼近本人文风，建议有 GPU）----------
# torch transformers peft trl datasets accelerate bitsandbytes

# ---------- 可选：PDF 文档摄取 ----------
# pypdf>=4.0.0
