const { db, _, moderate } = require('./common');
exports.main = async (event) => { const moderation=moderate(event.content); if(moderation.status==='blocked') return {ok:false,moderation}; const comment={post_id:event.post_id,user_id:event.user_id||'anonymous',content:String(event.content).trim(),reply_to:event.reply_to,like_count:0,status:moderation.status==='review'?'review':'published',created_at:Date.now()}; const ret=await db.collection('comments').add({data:comment}); if(comment.status==='published') await db.collection('posts').doc(event.post_id).update({data:{comment_count:_.inc(1)}}); return {ok:comment.status==='published',comment:{...comment,_id:ret._id},moderation}; };
