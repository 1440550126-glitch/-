const { db, moderate, parse } = require('./common');
exports.main = async (event) => { const moderation=moderate(event.text); if(moderation.status==='blocked') return {ok:false,moderation}; const parsed=parse(event.text,event.emotionTag,event.theme); const post={ user_id:event.user_id||'anonymous', text:String(event.text).trim(), emotion:parsed.emotion, theme:event.theme||parsed.scene, preview_config:parsed.preview_config, animation_manifest:parsed.animation_manifest, audio_config:{sounds:parsed.sound}, like_count:0, comment_count:0, share_count:0, collect_count:0, play_count:0, is_ai_post:!!event.is_ai_post, status:moderation.status==='review'?'review':'published', moderation, created_at:Date.now(), updated_at:Date.now() }; const ret=await db.collection('posts').add({data:post}); await db.collection('posts').doc(ret._id).update({data:{'animation_manifest.post_id':ret._id}}); return {ok:post.status==='published', _id:ret._id, post:{...post,_id:ret._id}, moderation}; };
